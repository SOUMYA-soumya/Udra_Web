name: udra_web
description: Web Administration Portal for Udra Challan
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0 # For calling your Flask backend
  shared_preferences: ^2.2.2 # For storing the secure login session token

flutter:
  uses-material-design: true
